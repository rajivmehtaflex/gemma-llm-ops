[project]
name = "gemma-llm-ops"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
training = [
    "datasets==4.3.0",
    "huggingface-hub>=1,<2",
    "peft==0.20.0",
    "tokenizers==0.22.2",
    "torch==2.12.1",
    "transformers==5.5.0",
    "trl==0.24.0",
    "unsloth==2026.9.4",
]

[project.scripts]
gemma-llm-ops = "gemma_llm_ops:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
